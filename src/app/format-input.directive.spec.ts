import { FormatInputDirective } from './format-input.directive';

describe('FormatInputDirective', () => {
  it('should create an instance', () => {
    const directive = new FormatInputDirective();
    expect(directive).toBeTruthy();
  });
});
